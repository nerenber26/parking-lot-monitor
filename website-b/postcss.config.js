import tailwind from 'tailwindcss';
import autoprefixer from 'autoprefixer';


/** @type {import("postcss").Config} */
export default {
    plugins: [
        tailwind,
        autoprefixer,
    ],
};